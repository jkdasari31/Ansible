# varables from command arguments
- name: variables from command arguments
  hosts: localhost
  tasks:
  - name: check variables
    ansible.builtin.debug:
      msg: "Hello {{PERSON}}, Good {{WISHES}}"